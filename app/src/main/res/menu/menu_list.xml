<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/player_btn"
        android:icon="@mipmap/player"
        android:title="Players"
        android:visible="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:showAsAction="always"/>

    <item
        android:id="@+id/chat_btn"
        android:icon="@drawable/ic_chat_black_24dp"
        android:title="@string/chat_btn"
        android:visible="true"
        app:showAsAction="always"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <item
        android:id="@+id/hist_btn"
        android:icon="@drawable/ic_history_black_24dp"
        android:title="@string/hist_btn"
        android:visible="true"
        app:showAsAction="always"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- Test button functionality suspended

    <item
        android:id="@+id/test_btn"
        android:icon="@drawable/ic_test_circle_black_24dp"
        android:title="@string/test_btn"
        android:visible="true"
        app:showAsAction="ifRoom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

        -->

</menu>